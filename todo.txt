release script
 │ 1. Toast vị trí khi chuyển ảnh — ten.jpg [3/12]                            │
 │                                                                            │
 │ Vì sao thích: Khi fullscreen hoặc trên Linux/macOS, user không biết đang   │
 │ ở ảnh nào. Lướt thư mục là flow chính; feedback này làm app “có ý          │
 │ thức”.                                                                     │
 │ Sửa ít: Trong load_index (sau Ok), gọi sẵn                                 │
 │ set_toast(self.gallery.title_label(), false). Toast infrastructure đã có   │
 │ trong draw_overlay (src/app.rs).                                           │
 │ Ước lượng: ~5–10 dòng.                                                     │
 │ ────────────────────────────────────────                                   │
 │                                                                            │
 │ 2. Hiện % zoom khi lăn chuột                                               │
 │                                                                            │
 │ Vì sao thích: Zoom là thao tác thứ hai sau lướt; hiện không có tín hiệu    │
 │ nào ngoài ảnh phóng to. Toast "125%" (hoặc fit khi về 100%) cho cảm giác   │
 │ kiểm soát ngay lập tức.                                                    │
 │ Sửa ít: Sau zoom_at_mouse trong handle_input, toast format!("{}%",         │
 │ (view.zoom_target * 100.0).round() as i32). Có thể kéo dài toast zoom      │
 │ ngắn hơn một chút hoặc tái dùng TOAST_DURATION.                            │
 │ Ước lượng: ~10–15 dòng.                                                    │
 │ ────────────────────────────────────────                                   │
 │                                                                            │
 │ 3. Phím ? / H bật overlay phím tắt                                         │
 │                                                                            │
 │ Vì sao thích: App gần như chỉ bàn phím; README có bảng phím nhưng trong    │
 │ app thì “đoán”. Một overlay tối giản (nền đen mờ + vài dòng) tăng          │
 │ discoverability cực mạnh mà không phá vibe tối giản — tắt bằng ?/Esc/H.    │
 │ Sửa ít: Flag show_help: bool + vài draw_text trong draw_overlay; không     │
 │ cần UI framework.                                                          │
 │ Nội dung gợi ý (ngắn): ← → lướt · bánh xe zoom · kéo pan · 0 reset ·       │
 │ Space fullscreen · R xoay · Del xóa · Ctrl+C copy · ? đóng                 │
 │ Ước lượng: ~40–60 dòng, chỉ trong app.rs (src/app.rs).                     │
 │ ────────────────────────────────────────                                   │
 │                                                                            │
 │ 4. Kéo-thả ảnh vào cửa sổ để mở / đổi thư mục                              │
 │                                                                            │
 │ Vì sao thích: README đã viết “kéo thả vào XemAnh” nhưng code chưa xử lý    │
 │ dropped_file (grep trống). Vá đúng lời hứa: giữa session đổi ảnh/thư mục   │
 │ không cần thoát app.                                                       │
 │ Sửa ít: Mỗi frame kiểm tra macroquad dropped files → nếu là ảnh hỗ trợ,    │
 │ Gallery::from_path (hoặc tái quét thư mục chứa file) rồi load_index.       │
 │ Toast "Opened …".                                                          │
 │ Ước lượng: ~30–50 dòng (app.rs (src/app.rs) + có thể tái dùng gallery.rs   │
 │ (src/gallery.rs)).                                                         │
 │ ────────────────────────────────────────                                   │
 │                                                                            │
 │ 5. Prefetch ảnh kế / trước (cache 1 bên mỗi phía)                          │
 │                                                                            │
 │ Vì sao thích: Đây là “cảm giác nhanh” lớn nhất của product promise Mở      │
 │ ảnh là xem ngay khi lướt cả folder. Hiện load_index decode đồng bộ trên    │
 │ UI thread — ảnh lớn sẽ giật. Cache sẵn next/prev làm mũi tên cảm giác      │
 │ tức thì.                                                                   │
 │ Sửa vừa phải nhưng ROI cao: Struct nhỏ CachedImage { index, image,         │
 │ texture } (hoặc chỉ decode LoadedImage trước, upload texture khi cần).     │
 │ Sau mỗi load_index thành công, prefetch next_index/prev_index trên         │
 │ thread riêng (std::thread + channel) hoặc decode tuần tự nhẹ sau frame     │
 │ (đơn giản hơn, vẫn giúp nhiều nếu decode nhanh).                           │
 │ Khuyến nghị mặc định (ít rủi ro): Prefetch đồng bộ sau khi vẽ frame hiện   │
 │ tại cho 1 ảnh kế tiếp thôi (không async phức tạp) — ~40–80 dòng; nếu       │
 │ muốn mượt hơn với ảnh 20MP thì mới thêm thread.                            │
 │ Ước lượng: ~50–100 dòng (app.rs (src/app.rs), có thể tách cache.rs         │
 │ (src/cache.rs) nhỏ).        